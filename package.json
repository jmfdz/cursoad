{
  "name": "cursoad",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/generar-indice.mjs && vite",
    "build": "node scripts/generar-indice.mjs && vue-tsc -b && vite build",
    "build:ua": "node scripts/generar-indice.mjs && vue-tsc -b && vite build --mode ua && (cd dist && zip -qr -FS ../docs_ua.zip .)",
    "preview": "pnpm run build && wrangler dev",
    "deploy": "pnpm run build && wrangler deploy"
  },
  "dependencies": {
    "bootstrap": "^5.3.8",
    "vue": "^3.5.30",
    "vue-router": "^4.6.3"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.31.0",
    "@types/node": "^24.12.0",
    "@vitejs/plugin-vue": "^6.0.5",
    "@vue/tsconfig": "^0.9.0",
    "typescript": "~5.9.3",
    "vite": "^8.0.1",
    "vue-tsc": "^3.2.5",
    "wrangler": "^4.80.0"
  }
}
